import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css'; // Assuming an index.css might exist or be created
import App from './App'; // Assuming an App.js might exist or be created

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
